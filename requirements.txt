fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0
zeroconf>=0.130
obsws-python>=1.7
websocket-client>=1.7
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.2; sys_platform == "win32"      # winaudio + OBSBOT-UVC COM (auch via pycaw, hier explizit)
pygrabber>=0.2; sys_platform == "win32"     # OBSBOT-Kamera via rohes UVC (DirectShow-Enumeration)
pyserial>=3.5                               # Arduino-HID-Bridge (send_via=arduino): serieller Port zum HID-Chip
