# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node / Vite
node_modules/
web/dist/

# Runtime / local state (per-install)
rigzdeck/runtime/
rigzdeck/static/
*.log

# Build / packaging (PyInstaller-Artefakte; eigene Spec + packaging/ bleiben getrackt)
build/
dist/
*.spec
!RigzDeck.spec

# Android-Signier-Keystore (kommt via CI-Secret ANDROID_KEYSTORE_B64, NIE committen)
*.keystore
*.jks

# Lokale APK-Ablagen (Build-/Download-Artefakte; APKs haengen via CI am Release)
apk/
apk_new/
